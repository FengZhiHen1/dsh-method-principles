# dsh-method-principles 待办

> 全库唯一待办清单，只装未完成事项。事项解决后就地写入拥有该事实的文档，并从此处移除。

## 未决事项

- **编码层文案定稿**：默认工程块已定稿（五个触发点：改行为前建基线/成功标准、声称修好前给根因或假设、重要结论区分观察与推断、破坏性操作前建范围与恢复路径、报告完成前披露验证边界；不含对抗式审查与"按比例"自判）。未决的是它是否真的有效，见下条。
- **默认工程块的实证效果**：尚无消融数据。需按附图第 3 条做对照实验（`routes: []` 为对照组），比较"未验证宣称完成 / 把假设说成事实 / 未查回归 / 破坏性操作前未建范围"四类失败率；无差异则删除该路由。
- **稳定环境生效确认**：插件已挂到 `web` profile；**待用户在 GUI 重启实例**后确认生效（重启后核对系统提示词折叠行是否含 `Working method` 五关）。
- **npm registry 发布**：当前只走 `github:` git 依赖。重访条件见 `technical-details/部署.md`。

## 延期事项

- **跨平台适配**：目标运行环境限定 Windows；插件本身无平台相关代码，无实际待办，仅在更换运行环境时复核。
